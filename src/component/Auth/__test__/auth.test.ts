'use client'
import React from 'react'
import Header from '@/pages/components/Header'
import {screen, render} from '@testing-library/react'

describe('Login component', () => {
    it('expects Login component to be rendered correctly', () =>{
        
    })
})

